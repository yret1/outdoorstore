import "../Styles/Header.css"
const Header = () => {

    return (
        <header className="header">
            <button>Menu</button>
            <button>Cart</button>
        </header>
    )
}

export default Header